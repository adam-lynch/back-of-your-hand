Example usage:

```
# Check all commentable files and error if any missing header
node lint.js --all

# Check all commentable files and add header if missing
node lint.js --all --write

# Check these files and error if any missing header
node lint.js example.js example.html

# Check these files and add header if missing
node lint.js --write example.js example.html
```
