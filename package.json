{
  "name": "back-of-your-hand",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build-all": "cd app && npm ci && npm run build && echo '/dist:' && ls -l ../dist && echo '/dist/assets:' && ls -l ../dist/assets && ./build/create-sentry-release.sh"
  },
  "devDependencies": {
    "glob": "^11.0.2",
    "husky": "^9.1.7",
    "ignore": "^5.3.2",
    "lint-staged": "^15.5.2"
  }
}
